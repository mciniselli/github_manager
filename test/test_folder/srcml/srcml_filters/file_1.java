package problem;

import java.util.Arrays;





public class removeDuplicatesInPlace {	

	private static int test() {
		int []arr;
		Arrays.sort(arr);
		int range = 0;
		boolean test=True;

		String TEST="cc"

		String cde = "cde";

		if (test){
			return range;
			return test;
		}

		if (!test){
			return range;
			return test;
		}

		if (range >= arr.length){
			return range;
		}

		if (range >= 10){
			return range;
		}

		if (arr.add("ciao") != null){
			return range;
		}

		if (range > test){
			return range;
		}

		if (!cde.equal("ciao") > test){
			return range;
		}

		if (!cde.equal(TEST) > test){
			return range;
		}

		if (!cde.equal(TEST, TEST) > test){
			return range;
		}

		return (arr.length - range);
	}

}

